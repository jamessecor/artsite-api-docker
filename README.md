# artsite-api-docker

This api makes use of docker and docker compose. It uses the out-of-the-box mongodb image and an express app. The purpose of the api is to persist information about artworks and send an email.
